{% extends 'base.html.twig' %}

{% block title %}Book{% endblock %}

{% block body %}

<div  class="container">
    <h1>Book</h1>

    <table class="table mb-5">
        <tbody>
            <tr>
                <th>Id : </th>
                <td>{{ book.id }}</td>
            </tr>
            <tr>
                <th>Titre : </th>
                <td>{{ book.title }}</td>
            </tr>
            <tr>
                <th>Auteur : </th>
                <td>{{ book.author }}</td>
            </tr>
            <tr>
                <th>Description : </th>
                <td>{{ book.description }}</td>
            </tr>
            <tr>
                <th>Couverture : </th>
                <td>{{ book.thumbnail }}</td>
            </tr>
            <tr>
                <th>ISBN : </th>
                <td>{{ book.ISBN }}</td>
            </tr>
        </tbody>
    </table>
<div class="flex justify-between items-center">
    
        <a class="link" href="{{ path('app_book_index') }}">Retour à la liste</a>
    
        <a class="btn" href="{{ path('app_book_edit', {'id': book.id}) }}">Modifier</a>
</div>

</div>
{% endblock %}
