{% extends 'base.html.twig' %}

{% block title %}Hello BookController!{% endblock %}

{% block body %}
<style>

</style>

{% if app.user %}

<div class="container">
    <a class="bg-indigo-950 text-white p-4 rounded-lg" href="{{ path('app_books') }}"> Retour à mes livres</a>

    <div class="mt-8">
        <h2 class="text-2xl text-red-300 font-bold">{{ book.title }}</h2>
        <p> {{ book.description }}</p>
        <p>-> {{ book.author }}</p>
        {# <p>-> {{ book.category_id_id }}</p> #}
    </div>
</div>

{% endif %}

{% endblock %}
