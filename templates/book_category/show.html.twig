{% extends 'base.html.twig' %}

{% block title %}BookCategory{% endblock %}

{% block body %}
<div  class="container">

    <h1>Catégorie :</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id : </th>
                <td>{{ book_category.id }}</td>
            </tr>
            <tr>
                <th>Catégorie : </th>
                <td>{{ book_category.category }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_book_category_index') }}">back to list</a>

    <a href="{{ path('app_book_category_edit', {'id': book_category.id}) }}">edit</a>

    {{ include('book_category/_delete_form.html.twig') }}

</div>
{% endblock %}
